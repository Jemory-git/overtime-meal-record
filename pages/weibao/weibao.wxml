<view class="weibaozonge">
    <text class="key">未报总额：</text>
    <text class="value">{{weibaoAccount}}</text>
</view>
<block wx:for="{{list}}" wx:key="{{item}}">
    <item itemdata="{{item}}" bind:onclickyibao="onclickyibao"></item>
</block>
